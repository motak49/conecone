import numpy as np
from PIL import Image

def create_embedding(image_path: str) -> np.ndarray:
    """
    画像から embedding を生成
    ※ ここは既存実装をそのまま移動する想定
    """
    # ダミー（既存処理に差し替えてOK）
    return np.random.rand(512)
